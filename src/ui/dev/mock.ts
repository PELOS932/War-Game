/**
 * DEV ONLY — mock implementations of the simulation, renderer and world used
 * to develop and screenshot the UI in isolation (see dev/ui.html).
 */
export { buildMockWorld } from './mockWorld';
export { createMockGame } from './mockGame';
export { createMockRenderer } from './mockRenderer';
